import { MobilePageFrame } from './MobilePageFrame';
import type { MobilePageProps } from './types';

export function MyEdgeAnalyzerMobile({ data }: MobilePageProps) {
  void data;
  return <MobilePageFrame title="My Edge Analyzer" description="Structure mobile dédiée — contenu à construire page par page." />;
}
