import { MobilePageFrame } from './MobilePageFrame';
import type { MobilePageProps } from './types';

export function AIAnalysisMobile({ data }: MobilePageProps) {
  void data;
  return <MobilePageFrame title="Analyse IA" description="Structure mobile dédiée — contenu à construire page par page." />;
}
