import { MobilePageFrame } from './MobilePageFrame';
import type { MobilePageProps } from './types';

export function TradeJournalMobile({ data }: MobilePageProps) {
  void data;
  return <MobilePageFrame title="Trade Journal" description="Structure mobile dédiée — contenu à construire page par page." />;
}
